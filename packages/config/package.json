{
  "name": "@trello-cli/config",
  "version": "1.0.0-alpha.1",
  "description": "",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsc --build tsconfig.build.json",
    "dev": "tsc --build tsconfig.build.json --watch --preserveWatchOutput",
    "prepack": "pnpm build"
  },
  "keywords": [],
  "author": "Michael Heap <m@michaelheap.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest-when": "^3.5.2"
  }
}
